import {
  Placement as InternalPlacement,
  Popover,
  PopoverProps as InternalPopoverProps,
} from "./Popover";

export type Placement = InternalPlacement;
export type PopoverProps = InternalPopoverProps;

export { Popover };
